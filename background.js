function clickHandler(){

  /*chrome.runtime.sendMessage({msg:'getcolor'}, function(response){
      document.getElementById('bgcolor').innerText = response.color;
    });
  chrome.tabs.query({active: true, currentWindow: true},function(tabs){
    
  });*/
  
}
